{
  "name": "pizza-store",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/pizza-store",
  "projectType": "application",
  "tags": [],
  "// targets": "to see all targets run: nx show project pizza-store --web",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": [
        "{workspaceRoot}/dist/apps/pizza-store/.next/!(cache)/**/*",
        "{workspaceRoot}/dist/apps/pizza-store/.next/!(cache)"
      ],
      "options": {
        "command": "next build",
        "cwd": "apps/pizza-store"
      },
      "dependsOn": ["^build"],
      "cache": true,
      "inputs": [
        "default",
        "^production",
        {
          "externalDependencies": ["next"]
        }
      ]
    },
    "dev": {
      "executor": "nx:run-commands",
      "options": {
        "command": "next dev",
        "cwd": "apps/pizza-store"
      }
    },
    "start": {
      "executor": "nx:run-commands",
      "options": {
        "command": "next start",
        "cwd": "apps/pizza-store"
      },
      "dependsOn": ["build"]
    },
    "serve-static": {
      "executor": "nx:run-commands",
      "options": {
        "command": "next start",
        "cwd": "apps/pizza-store"
      },
      "dependsOn": ["build"]
    }
  }
}
