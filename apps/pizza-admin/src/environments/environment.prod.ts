export const environment = {
  production: true,
  /** Тот же origin, что у сайта (nginx → pizza-api). Замените при отдельном домене API. */
  apiUrl: '/api',
  staticUrl: 'https://francescolucania.com/static/',
};
