{
  "name": "pizza-admin",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "prefix": "app",
  "sourceRoot": "apps/pizza-admin/src",
  "tags": [],
  "targets": {
    "extract-i18n": {
      "executor": "@angular-devkit/build-angular:extract-i18n",
      "options": {
        "buildTarget": "pizza-admin:build"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    },
    "serve-static": {
      "continuous": true,
      "executor": "@nx/web:file-server",
      "options": {
        "buildTarget": "pizza-admin:build",
        "spa": true
      }
    },
    "serve-ssr": {
      "continuous": true,
      "executor": "@angular-devkit/build-angular:ssr-dev-server",
      "configurations": {
        "development": {
          "browserTarget": "pizza-admin:build:development",
          "serverTarget": "pizza-admin:server:development"
        },
        "production": {
          "browserTarget": "pizza-admin:build:production",
          "serverTarget": "pizza-admin:server:production"
        }
      },
      "defaultConfiguration": "development"
    }
  }
}
